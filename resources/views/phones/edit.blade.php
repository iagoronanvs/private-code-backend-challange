@extends('layouts.app')

@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">{{ __('Cadastrar Telefone') }}</div>

                <div class="card-body">
                    <form method="POST"
                        action="{{ isset($phone)
                            ? route('clients.phones.update', ['client_id' => $client->id, 'id' => $phone->id])
                            : route('clients.phones.store', ['client_id' => $client->id]) }}">

                        @csrf

                        @if (isset($phone))
                            @method('PUT')
                        @endif

                        <div class="form-group row">
                            <label for="number" class="col-md-4 col-form-label text-md-right">{{ __('Number') }}</label>

                            <div class="col-md-6">
                                <input id="number" type="text" class="form-control @error('number') is-invalid @enderror" name="number" value="{{ isset($phone) ? $phone->number : old('number') }}" required autocomplete="number" autofocus>

                                @error('name')
                                    <span class="invalid-feedback" role="alert">
                                        <strong>{{ $message }}</strong>
                                    </span>
                                @enderror
                            </div>
                        </div>

                        <div class="form-group row mb-0">
                            <div class="col-md-6 offset-md-4">
                                <button type="submit" class="btn btn-primary">
                                    {{ __('Salvar') }}
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
